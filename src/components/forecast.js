import React from 'react';

const Forecast = props => {
  console.log(props);
  return (
    <React.Fragment>
      <h3>Forecast for next few days</h3>
    </React.Fragment>
  );
};

export default Forecast;
